# 3D-model-render-threejs
This application has been developed to realize how a 3D model (.stl file) can be rendered through Threejs.

To run the application locally, run the following commands-
```
npm install
npm start
```